
/* sensor
on human
    if app in network 5min or we decided homecoming last 5min
        bro's tryin to leave
        take these stuffs
    else 
        welcome back homie
        gimme these stuffs
on stuff
    if stuff gone
        if leavin
            led off (state mutate)
        if comin
            led gimme (state mutate)
    if stuff here
        if leavin
            led takeit (state mutate)
        if comin
            led off (state mutate)
*/

